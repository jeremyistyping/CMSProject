# Simple Mode Configuration
# Set CASHBANK_SIMPLE_MODE=true to bypass SSOT journal creation for instant processing

CASHBANK_SIMPLE_MODE=true

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=accounting_system
DB_SSLMODE=disable

# Server Configuration
PORT=8080
GIN_MODE=release

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION_HOURS=24

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000